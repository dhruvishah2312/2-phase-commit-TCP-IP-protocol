It has been developed on eclipse.

First run the server, then the co-odinator and then the participants.
Send a string from the coordinator which will reflect on all participants.
Global commit, global abort and timeout all cases have been handled
